# Shell Basics

Minimal C command interpreter focused on command execution and I/O redirection.

**Concepts:** `execvp`, file descriptors, `open`, `close`, `dup2`, stdin/stdout redirection, working-directory prompt.

The original coursework uses a course-supplied `LineParser` module that is not present in the uploaded archive.
